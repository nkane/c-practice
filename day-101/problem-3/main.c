/*
 * Write a function named countlets() that return the number of letters in an
 * entered string. Digits, spsaces, punctuation, tabs, and newline characters
 * should not be included in the returned count.
 *
 */

#include <stdio.h>

int
main()
{
	return 0;
}
