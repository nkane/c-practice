/*
 * The sum of a series of consecutive numbers from 1 to n can be defined
 * recursively as:
 *	sum(1) = 1;
 *	sum(n) = n + sum(n - 1)
 *
 * Write a recursive C function that accepts n as an argument and calculates
 * the sum of the numbers from 1 to n.
 *
 */

#include <stdio.h>

int
main()
{
	return 0;
}
