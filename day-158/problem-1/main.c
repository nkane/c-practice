/*
 * Add a menu function to Program 13.6 that gives the user a choice of adding a name to
 * the queue, remving a name from the queue, or listing the contents of the queue without
 * removing any structures from it.
 *
 */

#include <stdio.h>

int
main()
{
    return 0;
}
